def calc(val):
    sum=None
    try:
      sum=vasl[0]+val[1]+val[2]
      if val[0]==100:
        result=val100:
          print("결과",result)
    except  IndexError:
          print('리스트의 인텍ㄷ스에 에러가 발생했습니다')
        
      else:
          Print('에러가 없습니다')  
          
    finally:
          print('sum', sum)
          
    try:
        fp=open("./dave")